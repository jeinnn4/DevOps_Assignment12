version: '3.5'
services:  
  prometheus:
    image: prom/prometheus:latest
    container_name: prometheus
    volumes:
      - ../monitoring/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - prometheus_data:/prometheus  # Persist Prometheus data  
    ports:
      - "9090:9090"
    networks:
      - elastic
    restart: always

  grafana:
    image: grafana/grafana:latest
    container_name: grafana
    volumes:
      - grafana_data:/var/lib/grafana
    environment:
      - GF_SECURITY_ADMIN_USER=admin
      - GF_SECURITY_ADMIN_PASSWORD=4fsd#dad2e  # Set a secure password
    ports:
      - "3000:3000" 
    networks:
      - elastic
    restart: always

  node-exporter:
    image: prom/node-exporter:latest
    container_name: node-exporter
    ports:
      - "9100:9100"
    networks:
      - elastic
    restart: always
    
  nginx:
    image: nginx:latest
    container_name: nginx
    ports:
      - "8080:80"
    networks:
      - elastic
    restart: always

  nginx-exporter:
    image: nginx/nginx-prometheus-exporter:latest
    container_name: nginx-exporter
    command:
      - -nginx.scrape-uri=http://nginx:80
    ports:
      - "9113:9113"
    networks:
      - elastic
    depends_on:
      - nginx
    restart: always
    
volumes:
  prometheus_data:
  grafana_data:

networks:
  elastic:
    driver: bridge

